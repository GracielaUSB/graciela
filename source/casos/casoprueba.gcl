program main begin

  proc sumar : (inout m : int, in n : int) 
  begin
      var ss : int; 
      {pre m >= 0 /\ n >= 0 pre}  
      |[
           ss := 548;
           writeln(ss)
      ]|  
      {post m <= m + n post}
  end


  func restar : (m : int, n : int) -> int
  begin
      5+5
  end

  |[
    var x : int;
    var y : int;
    var z : char;

    y := 10;
    x := 3;

    if x < y  -> |[ x := x + 1 ; sumar(454,5) ; x := x + 1 ; writeln(x) ]|
    [] x >= y -> skip
    fi

  ]|
end

