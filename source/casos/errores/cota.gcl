program main |[
  |[
      var foo, bar := 5, 9 : int;
      var baz, qux := true, false : boolean;

      {inv (%forall i : int | 0 <= i /\ i < bar | foo+1 < bar %) inv} 
      {bound qux bound}

      do (foo < bar) ->
      |[
          foo := foo + 1
      ]|
      od


  ]|
]|
